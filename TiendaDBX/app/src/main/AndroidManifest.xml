<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tiendadbx">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_tiendadbx"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_foreground_tiendadbx"
        android:supportsRtl="true"
        android:hardwareAccelerated="true"
        android:theme="@style/Theme.TiendaDBX">
        <activity
            android:name=".ActivityRUD"
            android:exported="true" />
        <activity
            android:name=".ActivityCreate"
            android:exported="true" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>